# 2026-09-07

## 文档同步 2026-09-07

**上游范围**: d347e703908d..b0a7d2ce3b4c

### 更新的文档章节

#### part3-core/ch11-agent-loop.md
- 新增"模型切换通知"小节：说明 `0.1.3-alpha.2` 起 agent 的 provider/model 中途变化时自动插入 user-role 通知消息的机制
- 描述通知消息格式 `[model changed: ...]` 及实现细节（`agent/pre-step` waterfall 监听、effort-only 变化不触发、失败请求重复通知等）
- 对应上游变更：
  - `48cc1cf1d feat(agent): announce model switches`
  - 系统提示词配置重构（persona → personaPrefix + personaSuffix）
  - Session 格式流式迁移实现（内部实现细节，用户可见行为不变）

#### part3-core/ch13-system-prompt-scope.md
- 新增"部署 persona 前缀与后缀"小节：说明 `0.1.3-alpha.2` 起配置字段从单一 `persona` 拆分为 `personaPrefix`（order `0`）和 `personaSuffix`（order `10200`）
- 描述第一方段落的渲染顺序：harness 身份 → persona 前缀 → 可复用指令 → 环境后缀
- 解释前缀/后缀分离对 KV Cache 复用的稳定性影响
- 对应上游变更：
  - `40792330c fix(system-prompt): keep model persona prefix and place cwd in suffix`
  - `e28862db5 fix(system-prompt): place environment facts after reusable instructions`
  - `50b0511fe Merge pull request #3466 from deepseek-harness/feat/system-prompt-surface-node-notes`

### 未影响文档的变更

以下上游变更未触发文档更新：

- **桌面应用 / Electron 支持**（`ba05b7d49 Merge pull request #3413 from deepseek-harness/feat/electron`）：新增功能但无现有章节涵盖桌面应用，不强行新增章节
- **从 Web UI 打开本地应用**（`9292dd8a2 feat(workspace): open the workspace in local apps from the web UI`）：UI 功能，无现有架构描述章节
- **Feedback OTel 上传**（`9ffe85a51 OTel uploads explicit feedback for all users by default`、`02a029e67 Feedback is recorded in the session log`）：反馈机制变更但无现有章节详细描述 feedback
- **Queue 发送状态 UI**（`a3ccbd4d9 fix(web): show sending state for queued submissions`）：UI 细节，不属于核心架构描述
- **Resume 修复**（`6ad01cbd8 fix(web): avoid repeated system prompt on resume`、`477ff9d5e fix(agent-loop): preserve resumed series boundaries`）：bug 修复，无新功能文档化
- **`str_replace_editor` 从默认工具移除**（`36a466514 feat(base): remove str_replace_editor from default tools`、`965adbb5c feat(sdk): disable str_replace_editor by default`）：预设变更，ch12 未提及默认工具集
- **Session 流式迁移**（`98d2baf2e docs(session): document streaming format migration`、`46196d6f9 perf(session-format): stream released v0-to-v2 migrations`）：内部实现优化，ch10 10.11 节已涵盖 v0→v1→v2 迁移机制
- **Agent Notes 归档**（大量 `implemented/` → `archived/` 移动）：内部笔记重组，不影响公开文档
- **Durable inbox recovery 重构**（`b0a7d2ce3 Merge pull request #2672 from deepseek-harness/xtr/durable-inbox-recovery`）：内部重构，ch11 已描述 inbox 机制
- **依赖版本升级**（`e379fa8bd release(dsh): 0.1.3-alpha.2`）：版本号变化，无架构影响
- **各种 bug 修复和测试更新**：不影响公开 API 或架构描述
