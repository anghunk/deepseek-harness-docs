# 2026-09-04


## 文档同步 2026-09-04

**上游范围**: 76fda729799f..d347e703908d

### 更新的文档章节

#### part1-architecture/ch3-architecture.md
- 更新 turn 流程图：反映 Session 格式 v2 的嵌入式 stream 机制
- 对应上游变更：
  - Session 格式 v2（`SESSION_FORMAT_VERSION = 2`）
  - 移除 `assistant/chunk` 事件，stream 直接嵌入 `assistant/message` 和 `assistant/attempt`
  - 新增 `agent/assistant-stream` 实时帧（进程本地，不持久化）

#### part3-core/ch10-session-log.md
- 更新 10.2 节 SessionEventMap 词汇表：
  - 移除 `assistant/chunk` 事件
  - `assistant/message` 新增 `stream: AssistantStreamRecord[]` 字段
  - 新增 `assistant/attempt` 事件（失败/重试 attempt 的持久 stream）
  - `session/end-seed` 新增 `{ inherited?: true }` 字段
- 更新 10.4 节派生规则：移除 `assistant/chunk` 的跳过说明，新增 `assistant/attempt`
- 更新 surface 元数据说明：v2 的 `assistant/message` 不再携带 `sourceEventSeqs`
- 重写 10.11 节：详细说明 Session 格式 v0→v1→v2 迁移机制
- 移除过时的"打包助手历史"章节（已被 v2 嵌入式 stream 取代）
- 更新 10.13 小结：反映 v2 格式变化
- 对应上游变更：
  - `feat(session): released session format migrations`
  - `feat(session): v2 embedded assistant streams`
  - `feat(session-persistence-jsonl): cross-process write-ownership lease`

#### part3-core/ch11-agent-loop.md
- 更新 turn/step 循环图：`llm/stream` 后接 `agent/assistant-stream` 帧序列
- 更新 step() 函数描述：反映 v2 流式处理（嵌入 stream 到 message/attempt）
- 更新文档勘误：反映新的事件序列
- 对应上游变更：
  - `agent/assistant-stream` 实时帧引入

#### part3-core/ch14-llm-seam.md
- 更新 ContentBlockMap：新增 `file` 内容块类型
- 对应上游变更：
  - `feat(llm): file content block for verbatim file attachments`

#### part3-core/ch15-capability-seams.md
- 新增 `ctx.fileUploads` 能力缝：Agent 作用域的分阶段文件上传
- 对应上游变更：
  - `feat(client): file-upload package and ctx.fileUploads service`

### 未影响文档的变更

以下上游变更未触发文档更新：
- Web UI 样式改进（clickable links、skill chips、file upload UI 细节）
- Skill 模糊搜索改进（属于 Web UI 行为，书籍未详细描述）
- Python SDK Windows x64 运行时（文档中未引用具体运行时目标数量）
- 各种 bug 修复和测试更新
- 依赖版本升级（`release(dsh): 0.1.3-alpha.1`）
- 内部重构（不影响公开 API 或架构描述）
