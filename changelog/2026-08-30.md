# 2026-08-30

## 上游提交范围

`cd5ef8148158..0a53fb55bea1`（2026-08-30，共 82 个提交）

## 主要变更

### 版本发布
- **dsh 0.1.2-alpha.2 发布**：包含本周期所有增量改进

### 核心子系统
- **会话日志**：`ignorable` 字段保留用于外部插件（PR #3087 被 revert），SQLite schema 18→20（物理行判别从 `is_packed` 改为 `ignorable` 列）
- **系统提示词**：`FIRST_PARTY_SECTION_ORDER` 常量改为 `getSectionOrder()`/`getContextOrder()` 方法，`PERSONA_ORDER` 导出移除
- **Agent Presets**：新增 `./display` 子路径与 `presetDisplayText` 折叠函数，用于预设名称本地化
- **Plugin Inventory**：设置页插件列表现在按 agent preset scope 分组，新增 `compositionInventory()` 函数

### Web 客户端
- **ConnectionIndicator**：`ConnectionBanner` 重命名为 `ConnectionIndicator`，新增连接恢复指示器（断联/连接中/恢复状态）
- **Permission Presets**：权限预设标签现在使用产品本地化文案（Read Only / Workspace Write / Full access），而非 kebab-case 标题大小写
- **API Gateway**：`RemoteError` 失败词汇收敛，`ctx.remote` 编程表面文档化

### 其他
- **依赖解耦**：运行时依赖所有权重构，跨包运行时中继移除
- **Vendor 更新**：cordis 4.0.2、cosmokit 1.8.3、schemastery 3.18.2 等

## 文档改动

本次同步**未修改任何章节文件**。原因：

1. **SQLite schema 18→20**：文档未涉及 SQLite 物理 schema 版本号
2. **ConnectionBanner→ConnectionIndicator**：文档未提及该 UI 原子组件
3. **Plugin Inventory 分组**：文档仅描述 `plugin-inventory` 包职责，未涉及设置页 UI 布局
4. **Permission Presets 本地化**：文档未描述权限预设 UI 标签细节
5. **系统提示词 API 变更**：文档描述的 `section()` API 保持不变，`FIRST_PARTY_SECTION_ORDER` 常量未被引用
6. **Agent Presets display 子路径**：文档未涉及该内部 API
7. **ignorable 字段保留**：ch10 已正确描述 `ignorable?: true`，无需更新
8. **版本引用**：文档中所有 `0.1.2-alpha.1` 引用均描述特性引入时机，非当前版本

## 未影响文档的变更

- **测试与 CI**：大量测试用例更新、Windows 修复、CI 可靠性改进
- **内部重构**：品牌 ID 工厂函数（`brandString<T>()`）、session projection 状态表拆分、API gateway 内部实现
- **文档维护**：Agent Notes 翻译配对、模块图刷新、子系统文档更新（仓库内部 docs/，非本书）
- **性能优化**：stream queues 线性化、session title 缓存修复、atomic-write Windows 重试
